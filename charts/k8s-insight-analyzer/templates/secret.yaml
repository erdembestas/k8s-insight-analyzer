{{- if .Values.llm.apiTokenSecretName }}
# Optional: user-supplied secret; chart does not populate token values by default
# Create a secret with key `token` and set llm.apiTokenSecretName to reference it.
{{- end }}
